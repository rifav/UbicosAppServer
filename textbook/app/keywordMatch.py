
class keywordMatch():
    def matchingMethod(self, message, selected_badge):
        print(selected_badge);
        if (selected_badge == 'brainstorm'):
            #match against brainstorm keywords
            #if match return true - else continue;
            return True;


        return False;


if __name__ == "__main__":
    keywordMatch.matchingMethod(None)